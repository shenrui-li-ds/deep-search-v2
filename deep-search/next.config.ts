import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com', 'www.reuters.com', 'www.cnbc.com', 'www.techcrunch.com', 'www.nextgov.com', 'www.example.com'],
  },
};

export default nextConfig;
